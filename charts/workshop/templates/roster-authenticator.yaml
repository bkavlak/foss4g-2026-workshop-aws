apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "workshop.authenticatorConfigMap" . }}
  labels:
    {{- include "workshop.labels" . | nindent 4 }}
data:
  roster_authenticator.py: |
{{ .Files.Get "files/roster_authenticator.py" | indent 4 }}
